Core Principles:
- Zero Dependencies: this repo has to contain everything source-wise so that the OS can be build.
- Minimalism: only added what is needed. The kernel should be as slick as achievable.

Self-allowed tooling for this project:
- rustc
- cargo
- just, as a reduced Makefile alternative
